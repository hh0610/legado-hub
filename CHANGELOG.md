# Changelog

格式大致遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。  
版本号遵循 [SemVer](https://semver.org/lang/zh-CN/)。

镜像通道见 README「镜像通道」：`beta` = 开发测试，`vX.Y.Z` / `latest` = 正式发行。

书源规则版本（Reading 名称里的 `0.x.y`）只在**正式发版**时递增；日常 beta 只推进 `lastUpdateTime` 时间戳，避免版本号刷屏。

## [Unreleased]

## [0.4.0] - 2026-09-14

书源规则版本：`0.0.30`。

### 新增

- 章节手动选源补全（#6 社区贡献，PR #7）：书库章节列表新增「手动选源」，只读扫描各候选源的匹配章节并返回净化长度、字数比、对齐相似度、被拒原因与首尾预览；人工采纳跳过自动字数/对齐门但保留净化、词库与审计管线，采纳章带「人工」徽章且自动更新不再覆盖，「处理本章」可重新接管
- 「处理本章」接口修复（PR #7）：手动端点改为事件循环内执行并显式映射章节字段，不再 500

### 修复

- 起点 Web 段评详情接入宿主加载入口，按指定页获取评论，修复详情无法正常加载的问题（#5）
- 段评与本章说的楼中楼在返回空回复或暂不支持完整回复时，不再统一误报“加载失败，点击重试”；已有回复可以展开，真实请求失败仍保留重试（#5）

### 已知限制

- **起点 Web 评论功能尚不完整**：目前只能展示评论列表附带的部分楼中楼回复，尚未接通全部回复及其分页加载。页面会明确提示暂不支持加载完整回复。

## [0.3.2] - 2026-09-06

书源规则版本：`0.0.29`。v0.3.1 紧急热修。

### 修复

- **v0.3.1 镜像插件抓取全灭**：上游 `primp` 2.0.0（ddgs 底层 HTTP 客户端）不再传递依赖 `h2`/`brotli`/`socksio`，而插件抓取器无条件以 `http2=True` 构造 httpx 客户端，导致所有源在发起请求前即抛 `ImportError`——表现为搜索/订阅/目录全部返回空（issue #4）。修复：`requirements.txt` 显式声明 `httpx[http2,brotli,socks]`；抓取器对 h2 缺失优雅降级到 HTTP/1.1 并输出告警，依赖漂移不再能砖化抓取

## [0.3.1] - 2026-09-06

书源规则版本：`0.0.28`。

### 修复

- Docker 端口映射（如 `4390:8765`、`4391:8766`）后，管理后台生成的专属链接与书源内的「订阅 / 书库」入口仍固化容器内端口。新增 `LEGADOHUB_READER_EXTERNAL_ORIGIN`（完整 origin 或裸端口）声明对外阅读地址；管理后台专属链接、书源基址、`8766` 旧书源兼容跳转统一走该解析
- 管理后台经反向代理 / HTTPS 访问时登录被 Origin 校验拒绝，表现如同"密码错误"。管理口默认信任内网网段代理转发的真实 IP 与协议；动态适配模式下放行同 Host 不同协议/端口的来源；被拒时页面直接给出双方 Origin 与对应配置指引，日志记录具体值
- 群晖 Container Manager / Portainer 网页端对 stdout/stderr 混流 json 日志解析存在缺陷、显示「无可用日志」：容器内所有日志（含 uvicorn 访问日志）统一输出到单一流

### 变更

- 前端开发依赖 semver 内升级（`npm audit fix`），清除 browserslist / nanoid / undici 高危通告

## [0.3.0] - 2026-08-04

书源规则版本：`0.0.27`。

### 新增

- 订阅源快照：按书源保存原始正文、净化正文、净化审计和抓取进度，支持断点续抓与本地重建
- 初次订阅为第三方候选源提供最多 60 秒、20 章的并行预取；正式处理优先复用快照，缺失时再按需抓取
- 完结书籍自动归档，书库新增“进行中 / 已完结”分类并停止归档书籍的订阅活动
- Browser Source Access Bridge 增加浏览器池、持久化浏览器资料、POST 表单和搜索引擎发现能力
- Docker 镜像内置起点 Web 官方源及全部可用第三方源，宿主挂载目录按插件 ID 补齐缺失项
- 书库新增完整性检查与本地修复入口，核对数据库、章节文件、来源快照及残留临时文件

### 变更

- 单书单源并发收紧，不同第三方源仍可并行；官方源与 Browser 源统一进入宿主队列和插件限流器
- 候选源目录在当前进程周期缓存，源映射刷新或进程重启时失效，避免每批章节重复加载
- 多源模糊对齐在严格三源、双侧锚点、最多 3 段和 2% 上限内剔除单源额外段；官方预览或字数复核失败时整次撤销，原始快照保持不变
- 句级顺序校验在两个参考来源一致时拒绝错序候选；双源冲突不定责、不删改，繁简归一只用于比较指纹
- 重建默认只使用本地快照；管理员明确选择重新抓取时才访问源站
- 订阅任务超时按处理批次扩展，失败快照会中断并等待后续周期继续
- 书库和详情页实时显示章节处理、候选源下载、失败与归档进度

### 修复

- 首次订阅被固定 180 秒外层超时截断，以及候选源预取阻塞正常章节处理
- 同一插件更换书籍映射后旧快照运行重复计数，部分失败时进度错误显示 100%
- 订阅中书籍无法由有权限用户删除，以及重算、重建、源映射刷新反馈不明显
- 旧编码中文站点在缺少或错误声明 charset 时出现乱码
- 本地快照重建逐章重复加载全部插件元数据，导致千章书籍重建耗时过长
- 本地重建在候补正文失去官方锚点后仍保留旧可读路径，以及降级预览沿用旧候补对齐元数据
- Cloudflare 页面未被识别、浏览器会话未复用，以及声明自动代理的搜索链路没有执行直连后代理重试
- 页面过长时侧栏拉伸、书库与详情进度需刷新、设置优先级和状态反馈样式不一致
- 候选章节按目录位置误选无关章节并重复等待空正文，以及错位快照被后续任务继续复用

### 书源

- 新增 `aiqu654_com`、`aishu99_com`、`czbooks_net`、`dxtxt_cc`、`feishubook_com`、`ixdzs8_com`、`lingdiankanshu_com`、`qiexs_cc`、`quexs_org`、`suixkan_com`、`xinjianpan_com`、`xs63b_com`、`yeban360_com`
- 完整复核正式第三方插件的搜索、详情、目录、正文、编码、分页与限流声明，并补充 fixture smoke
- 候选 `novel_ip_23_225_143_226` 与 `lingdiankanshu_com` 同域重复，未重复投产
- 归档并移除失效的 `22biqu_com`；移除仅复用 `xbiqugu.com` 镜像的重复插件 `xbiquzw_net`
- 起点 Web 官方插件同步自 `QDFCCKK`，App 官方插件仍不进入公开镜像

### 部署

- 官方与第三方插件目录均改为可写覆盖层；镜像启动只补齐宿主缺失的插件 ID，不覆盖已有版本
- 保留非 root 运行、PUID/PGID 权限修复、Bridge 网络和 Docker 日志轮转

## [0.2.0] - 2026-07-24

书源规则版本：`0.0.26`。

### 新增

- 专属书源强制携带 `code`，搜索、目录与正文统一静默兑换用户会话
- Docker 部署变量 `LEGADOHUB_PUBLIC_BASE_URL` 可预设公网专属书源地址；后台设置保存后立即覆盖该默认值

### 变更

- 公网 Host 是否可达交由防火墙、WAF 或反向代理控制；应用继续校验 Host 语法并阻止公网客户端伪造局域网 Host
- Reading 登录页精简为“订阅 / 书库”，专属书源不再要求用户重复输入授权码
- 公网与局域网书源、搜索状态和会话保持独立，避免客户端合并同一本书的两条网络链路

### 修复

- 书源内控制台链接错误指向管理端口，现统一使用阅读端口
- Reading 请求头异步调用阻断登录面板，以及不同客户端对登录脚本兼容不一致
- 用户凭证弹窗在 HTTP 局域网环境无法复制，并统一创建、重置后的凭证展示
- 部署变量与后台“公网书源地址”契约漂移；恢复“后台设置 > 环境变量 > 请求地址”的链接生成优先级
- 升级 React Router 至 7.18.1，修复公开披露的重定向、XSS 与反序列化安全问题

### 镜像

| 通道 | Tag |
|------|-----|
| 正式 | `xzixmn/legado-hub:v0.2.0`、`xzixmn/legado-hub:latest` |
| 开发测试 | `xzixmn/legado-hub:beta` |
| 精确定位 | `xzixmn/legado-hub:sha-<commit>` |

## [0.1.0] - 2026-07-24

首个正式发行。书源规则版本：`0.0.20`。

### 新增

- 个人专属书源 / 订阅链接：授权码写入 `?code=`，Reading 静默鉴权；Web 订阅页经 `/api/auth/access/enter` 免再输码
- 用户管理：创建/重置授权码时展示可复制的公网/内网专属链接
- 公网与局域网双书源身份（`LegadoHub` / `LegadoHub-LAN`）与搜索分轨
- 阅读口设置「允许的公网地址」优先于环境变量引导

### 修复

- 管理台 HTTP 局域网下复制按钮（Clipboard API 回退）
- 多种 69 书吧混淆水印（含 `6.9ꁘ書吧`、`6=9+` 残片等）
- 绑定专属源登录页仅保留「订阅管理 / 退出」；请求头静默兑换会话，减少「点击登录去授权」
- 插件正则内联 flags 等稳定性问题

### 文档

- README：公网部署、专属链接、镜像通道说明

### 镜像

| 通道 | Tag |
|------|-----|
| 正式 | `xzixmn/legado-hub:v0.1.0`、`xzixmn/legado-hub:latest` |
| 开发测试 | `xzixmn/legado-hub:beta`（随 main 滚动） |
| 精确定位 | `xzixmn/legado-hub:sha-<commit>` |

[0.1.0]: https://github.com/XziXmn/legado-hub/releases/tag/v0.1.0
[0.2.0]: https://github.com/XziXmn/legado-hub/releases/tag/v0.2.0
[0.3.0]: https://github.com/XziXmn/legado-hub/releases/tag/v0.3.0
